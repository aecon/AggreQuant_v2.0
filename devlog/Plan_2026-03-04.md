# Plan 2026-03-04

## Tasks

- [x] Use LUT for consecutive relabeling in `FilterBasedSegmenter` (fast O(N), no re-labeling)
- [x] Fix hole-filling order in `FilterBasedSegmenter`: label after `remove_small_holes` per skimage recommendation
- [x] Rewrite `_match_cells_to_nuclei`: greedy matching by nucleus occupancy fraction, drop unmatched cells and nuclei
- [x] Sync `nuclei_labels` in-place after cell matching (zero out unmatched nuclei inside `CellposeSegmenter.segment()`)
- [x] Delete all existing tests and write new unit tests for aggregate, nuclei, and cell segmentation
- [x] Fix stardist LUT dtype to uint16; fix deprecated `binary_dilation` → `dilation`
