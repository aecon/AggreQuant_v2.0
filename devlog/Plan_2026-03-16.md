# Plan 2026-03-16

## nn/ Module Code Review & Simplification

File-by-file walkthrough of the `nn/` module focused on over-engineering and simplicity. Changes are made with user approval at each step.

### Completed

- [x] **`nn/utils.py`** — Removed unnecessary `__all__`
- [x] **`nn/architectures/blocks/conv.py`** — Added inline comments explaining Conv2d/BatchNorm/ReLU, removed duplicate forward docstrings
- [x] **`nn/architectures/blocks/residual.py`** — Removed duplicate forward docstrings
- [x] **`nn/architectures/blocks/attention.py`** — Flagged `bias=True` before BatchNorm (inconsistent, fix deferred)
- [x] **`nn/architectures/blocks/se.py`** — Removed duplicate forward docstrings
- [x] **`nn/architectures/blocks/cbam.py`** — Removed duplicate forward docstrings; flagged hard assert in SpatialAttention (fix deferred)
- [x] **`nn/architectures/blocks/eca.py`** — Removed duplicate forward docstring; flagged `kernel_size: int = None` type hint (fix deferred)
- [x] **`nn/architectures/blocks/convnext.py`** — Removed duplicate forward docstring
- [x] **`nn/architectures/blocks/aspp.py`** — Removed forward docstrings from ASPP and LightASPP, kept ASPPBridge
- [x] **`nn/architectures/unet.py`** — Renamed `ModularUNet` → `UNet`, removed `**kwargs` (silent typo trap), updated all references
- [x] **`nn/architectures/registry.py`** — Removed `"class"` field indirection, direct `UNet` import, removed UNet++ deferred comment
- [x] **`nn/data/augmentation.py`** — Complete rewrite from albumentations to torchvision.transforms.v2; then simplified:
  - Removed `get_test_time_augmentation` (conceptually wrong — TTA needs deterministic transforms, not random)
  - Removed `get_validation_augmentation`, `get_light_augmentation`, `get_heavy_augmentation` (unnecessary presets)
  - Removed `AUGMENTATION_PRESETS`, `get_augmentation_by_name`, `list_augmentation_presets`
  - Removed `**kwargs` from `get_augmentation_by_name` (silent typo trap)
  - Removed `image_size` parameter (sizing handled by dataset/inference, not augmentation)
  - Kept: 3 custom transforms, `_wrap_inputs`, `apply_transform`, `get_training_augmentation`
- [x] **`nn/data/dataset.py`** — Major redesign:
  - Removed `AggregateDataset` (random-crop-on-the-fly has data leakage between train/val)
  - Removed `InferenceDataset` (redundant with `inference.py`)
  - Added `extract_patches()` — grid-cuts full images into non-overlapping patches, saves to disk
  - Updated `PatchDataset` — now takes a list of file paths (not a directory) for flexible train/val splits
  - Rewrote `create_dataloaders()` — takes patch dir, shuffles file list with seed, splits into train/val
  - Removed `__all__`, unused `import os`
- [x] **Documentation** — Added Training Data Pipeline section to `nn_building_blocks.md`

### In Progress

- [x] **`nn/training/losses.py`** — Removed `__all__`. BoundaryLoss flagged for later (see docs).
- [x] **`nn/training/trainer.py`** — Removed `train_model` convenience function, unused imports, `__all__`. Added `weights_only=True` to `torch.load`. Fixed early stopping to skip when no val_loader.
- [x] **`nn/evaluation/metrics.py`** — Removed `f1_score` (identical to `dice_score`), unused imports, `__all__`. Added `soft_dice_score`. Redesigned `SegmentationMetrics` for single-pass confusion matrix. Made `evaluate_model` configurable with `metrics` parameter. Renamed functions for clarity (`precision` → `precision_score`, etc.).
- [x] **`nn/inference.py`** — Fixed docstring `torch.load` example. Renamed `min_area`/`max_hole_area` → `remove_objects_below`/`fill_holes_below` for clarity.

### Deferred Issues (flagged but not yet fixed)

- [ ] `attention.py`: `bias=True` on Conv2d layers before BatchNorm (lines 63, 69, 75)
- [ ] `cbam.py`: Hard assert `kernel_size in (3, 7)` in SpatialAttention
- [ ] `eca.py`: `kernel_size: int = None` should be `Optional[int] = None`
- [ ] `unet.py`: Task 15 — Complete `UNet.get_config()` (missing most params)
- [ ] `losses.py`: Inspect and fix `BoundaryLoss` — see literature review in `nn_building_blocks.md`

### Pending (from previous plan)

- [ ] Task 11: Unit tests — need update for dataset.py redesign (remove AggregateDataset/InferenceDataset tests, add extract_patches + PatchDataset + create_dataloaders tests)
- [ ] Task 12: Integrate `nn/inference.py` into main pipeline
- [ ] Task 13: Training script for 7-variant ablation benchmark
- [ ] Task 14: Augmentation unit tests (rewrite done, tests not yet written)
- [ ] Commit all current changes (19 files modified, -834/+400 lines)
